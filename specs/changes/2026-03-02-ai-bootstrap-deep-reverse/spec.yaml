# EvoSpec Change Specification
# Schema: https://evospec.dev/schemas/spec.schema.json

id: "ai-bootstrap-deep-reverse"
title: "AI Bootstrap Prompt + Deep Reverse Engineering"
zone: "hybrid"  # edge | hybrid | core
change_type: "improvement"  # experiment | improvement | bugfix
status: "draft"     # draft | proposed | accepted | in-progress | completed | abandoned | superseded
created_at: "2026-03-02"
updated_at: "2026-03-02"

# --- Classification (output of `evospec classify`) ---
classification:
  touches_persistence: false
  touches_auth: false
  touches_billing: false
  touches_audit: false
  touches_multi_tenancy: false
  crosses_context_boundary: true
  is_hypothesis: false
  reversibility: "trivial"  # trivial | moderate | difficult | irreversible
  risk_level: "low"          # low | medium | high | critical
  rationale: "New CLI command + enhanced reverse engineering. Crosses reverse-engineering → agent-integration boundary. No persistence, auth, or billing. Fully reversible — adds new commands without breaking existing ones."

# --- Invariant Impact (auto-populated by `evospec classify` or MCP check_invariant_impact) ---
invariant_impact:
  conflicts: []
  entities_touched:
    - "CanonicalWorkflow"
    - "ProjectConfig"
  contexts_touched:
    - "reverse-engineering"
    - "agent-integration"
    - "cli"
  reviewed: true

# --- Domain (required for core, optional for hybrid) ---
bounded_context: "reverse-engineering"

# --- Team Topology ---
ownership:
  team: "evospec-core"
  team_type: "platform"
  interaction_mode: "x-as-a-service"

# --- Strategic Fit (Roger Martin) ---
strategic_fit:
  winning_aspiration: "Be the standard spec-driven delivery toolkit for AI-assisted development"
  where_to_play: "Teams using AI coding agents that need to onboard existing projects with zero manual configuration"
  how_to_win: "Instant AI bootstrap — one command gives any agent full project context, eliminating the 3-minute exploration tax"
  capability_strengthened: "AI agent onboarding speed and reverse engineering depth"

# --- Invariants (required for core) ---
invariants: []

# --- Fitness Functions (required for core) ---
fitness_functions: []

# --- ADRs ---
adrs: []

# --- Traceability (can be auto-generated by `evospec reverse`) ---
traceability:
  endpoints: []
  tables: []
  modules:
    - "evospec.cli.main"
    - "evospec.core.prompt"
    - "evospec.reverse.api"
    - "evospec.reverse.db"
    - "evospec.reverse.cli"
    - "evospec.reverse.deps"
    - "evospec.core.init"
    - "evospec.mcp.server"
  events: []
  migrations: []

# --- Discovery (not applicable — this is an improvement) ---
discovery:
  outcome: ""
  opportunity: ""
  iteration: 1
  cadence: "weekly"
  next_checkpoint: ""
  assumptions: []
  experiments: []
  kill_criteria: ""
  kill_deadline: ""
  learnings: []

# --- Design Thinking (edge/hybrid) ---
design_thinking:
  current_phase: "define"
  problem_statement: "AI agents waste 3+ minutes exploring evospec source code to learn CLI commands and project context before they can configure a project. This friction prevents adoption."
  ideas_explored: 3
  empathy_artifacts:
    - "/Users/lohmann/Downloads/evospec-ai-bootstrap-proposal.md"
  prototypes: []

# --- Organizational Risk (Hogan) ---
organizational_risk:
  derailer_risks:
    - "Over-engineering the detection logic instead of shipping a simple prompt first"
  cultural_constraints: []
  mitigation: "Ship the simple `evospec prompt` first, then iterate on detection depth."
