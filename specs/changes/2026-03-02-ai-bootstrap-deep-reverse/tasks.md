# Tasks: AI Bootstrap Prompt + Deep Reverse Engineering

> Zone: **hybrid** | Type: **improvement** | Status: **completed**
>
> Generated by `/evospec.tasks` from spec artifacts.

---

## Phase 1: Setup

- [x] T001 [P] [Setup] Create `src/evospec/core/prompt.py` with `ProjectDetection` and `GitInfo` dataclasses
- [x] T002 [P] [Setup] Create `src/evospec/templates/prompts/` directory and `bootstrap.md` Jinja2 template

## Phase 2: Detection Logic

- [x] T003 [Foundation] Implement `detect_language()` in `src/evospec/core/prompt.py` — scan for build files (pom.xml, build.gradle, package.json, go.mod, pyproject.toml, requirements.txt, Cargo.toml)
- [x] T004 [Foundation] Implement `detect_framework()` in `src/evospec/core/prompt.py` — parse build file dependencies to match framework (spring, fastapi, django, flask, express, nextjs, nestjs, gin, echo, fiber, chi, hono, fastify)
- [x] T005 [Foundation] Implement `detect_orm()` in `src/evospec/core/prompt.py` — identify ORM from dependencies or source imports (JPA, SQLAlchemy, Django ORM, GORM, Prisma, TypeORM, Sequelize)
- [x] T006 [Foundation] Implement `detect_source_dirs()` in `src/evospec/core/prompt.py` — detect source directories by language convention (src/main/java, src/, app/, lib/)
- [x] T007 [Foundation] Implement `detect_project_name()` in `src/evospec/core/prompt.py` — extract name from build file metadata (pom.xml artifactId, package.json name, pyproject.toml project.name, go.mod module)
- [x] T008 [Foundation] Implement `detect_project_stack()` in `src/evospec/core/prompt.py` — orchestrate all detection functions into a single `ProjectDetection` result

## Phase 3: Git History Analysis

- [x] T009 [Foundation] Implement `analyze_git_history()` in `src/evospec/core/prompt.py` — run `git log`, `git shortlog`, `git diff --stat` to produce `GitInfo` (recent_commits, contributors, hot_files, primary_language_pct)
- [x] T010 [Foundation] Add graceful degradation in `analyze_git_history()` — return `None` if `git` binary not found or not a git repo (BOOT-INV-002)

## Phase 4: Prompt Generation

- [x] T011 [Core] Write `src/evospec/templates/prompts/bootstrap.md` Jinja2 template — CLI reference table, step-by-step init/reverse/curate guide, workflow commands, ~2-3KB output
- [x] T012 [Core] Implement `generate_bootstrap_prompt()` in `src/evospec/core/prompt.py` — render template with version, detection results, git info
- [x] T013 [Core] Implement `generate_bootstrap_json()` in `src/evospec/core/prompt.py` — structured JSON output with commands, detection, post_init_files (BOOT-INV-004)

## Phase 5: CLI Integration

- [x] T014 [Integration] Register `prompt` command in `src/evospec/cli/main.py` — `evospec prompt [--detect] [--format markdown|json]`
- [x] T015 [Integration] Ensure `evospec prompt` works without `evospec.yaml` existing (BOOT-INV-001) — do NOT call `find_project_root()` or require config

## Phase 6: Enhanced Init

- [x] T016 [Integration] Update `src/evospec/core/init.py` — accept optional `ProjectDetection` and pre-fill `reverse.framework`, `reverse.source_dirs` in generated `evospec.yaml`
- [x] T017 [Integration] Update `src/evospec/cli/main.py` `init` command — add `--detect` flag that runs project detection and passes results to init

## Phase 7: MCP Bootstrap Resource

- [x] T018 [Integration] Add `evospec://bootstrap` resource in `src/evospec/mcp/server.py` — returns bootstrap prompt via MCP for pre-init agent discovery

## Phase 8: Tests

- [x] T019 [P] [Guardrails] Add test for `detect_project_stack()` with a mock Spring Boot project (pom.xml + spring-boot-starter) in `tests/test_prompt.py`
- [x] T020 [P] [Guardrails] Add test for `detect_project_stack()` with a mock FastAPI project (pyproject.toml + fastapi dep) in `tests/test_prompt.py`
- [x] T021 [P] [Guardrails] Add test for `detect_project_stack()` with a mock Next.js project (package.json + next dep) in `tests/test_prompt.py`
- [x] T022 [P] [Guardrails] Add test for `detect_project_stack()` with a mock Go/Gin project (go.mod + gin dep) in `tests/test_prompt.py`
- [x] T023 [P] [Guardrails] Add test for `analyze_git_history()` graceful degradation (no git) in `tests/test_prompt.py`
- [x] T024 [P] [Guardrails] Add test for `evospec prompt` CLI output (markdown format) in `tests/test_prompt.py`
- [x] T025 [P] [Guardrails] Add test for `evospec prompt --format json` output (valid JSON) in `tests/test_prompt.py`
- [x] T026 [Guardrails] Add test for `evospec prompt` without evospec.yaml (BOOT-INV-001) in `tests/test_prompt.py`
- [x] T027 [Guardrails] Run all existing tests to verify no regressions: `pytest tests/ -v`

## Phase 9: Documentation

- [x] T028 [P] [Polish] Update `docs/getting-started.md` — add `evospec prompt` to the workflow and quick start
- [x] T029 [P] [Polish] Update `README.md` — add `evospec prompt` to CLI reference table
- [x] T030 [P] [Polish] Update `specs/domain/features.yaml` — add feat-016 "AI Bootstrap Prompt" and feat-017 "Deep Project Detection"

---

## Invariant-to-Task Mapping

| Invariant | Statement | Tasks |
|-----------|-----------|-------|
| BOOT-INV-001 | `evospec prompt` works without evospec.yaml | T015, T026 |
| BOOT-INV-002 | `--detect` gracefully degrades without git | T010, T023 |
| BOOT-INV-003 | Detection is read-only | T003-T009 (by design — no writes) |
| BOOT-INV-004 | `--format json` outputs valid JSON | T013, T025 |

---

## Dependency Graph

```
Phase 1 (Setup)
  T001 ──┐
  T002 ──┤
         ▼
Phase 2 (Detection)
  T003 → T004 → T005 → T006 → T007 → T008
         ▼
Phase 3 (Git)
  T009 → T010
         ▼
Phase 4 (Prompt)
  T011 → T012 → T013
         ▼
Phase 5 (CLI)
  T014 → T015
         ▼
Phase 6 (Init)       Phase 7 (MCP)
  T016 → T017         T018
         ▼               ▼
Phase 8 (Tests)
  T019 ∥ T020 ∥ T021 ∥ T022 ∥ T023 ∥ T024 ∥ T025 → T026 → T027
         ▼
Phase 9 (Docs)
  T028 ∥ T029 ∥ T030
```

---

## Summary

| Metric | Value |
|--------|-------|
| Total tasks | 30 |
| Phases | 9 |
| Parallel opportunities | 14 tasks (T001-T002, T019-T025, T028-T030) |
| Invariant coverage | 4/4 (100%) |
| Estimated effort | ~4-6 hours AI-assisted |

**MVP Scope** (phases 1-5): `evospec prompt [--detect]` working end-to-end with detection for all supported languages. 18 tasks.

**Full Scope** (all phases): includes enhanced init, MCP resource, full test suite, documentation. 30 tasks.
