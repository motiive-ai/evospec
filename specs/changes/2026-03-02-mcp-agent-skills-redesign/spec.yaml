# EvoSpec Change Specification
# Schema: https://evospec.dev/schemas/spec.schema.json

id: "mcp-agent-skills-redesign"
title: "MCP + Agent Skills Redesign"
zone: "hybrid"  # edge | hybrid | core
change_type: "improvement"  # experiment | improvement | bugfix
status: "draft"     # draft | proposed | accepted | in-progress | completed | abandoned | superseded
created_at: "2026-03-02"
updated_at: "2026-03-02"

# --- Classification (output of `evospec classify`) ---
classification:
  touches_persistence: false
  touches_auth: false
  touches_billing: false
  touches_audit: false
  touches_multi_tenancy: false
  crosses_context_boundary: true
  is_hypothesis: false
  reversibility: "moderate"  # trivial | moderate | difficult | irreversible
  risk_level: "medium"        # low | medium | high | critical
  rationale: "Crosses agent-integration and MCP server bounded contexts. Restructures how AI agents consume EvoSpec workflows (Skills) and domain data (MCP). Additive — existing platform-specific files kept as fallback."

# --- Invariant Impact (auto-populated by `evospec classify` or MCP check_invariant_impact) ---
invariant_impact:
  conflicts: []
  entities_touched:
    - "WorkflowSpec"
    - "AgentFile"
    - "MCPResource"
    - "MCPTool"
    - "Skill"
    - "ContractFile"
    - "UpstreamAPI"
  contexts_touched:
    - "agent-integration"
    - "mcp-server"
  reviewed: true

# --- Domain (required for core, optional for hybrid) ---
bounded_context: "agent-integration"

# --- Team Topology ---
ownership:
  team: "evospec-core"
  team_type: "platform"
  interaction_mode: "x-as-a-service"

# --- Strategic Fit (Roger Martin) ---
strategic_fit:
  winning_aspiration: "EvoSpec is the spec-driven delivery toolkit that AI agents use natively across all platforms"
  where_to_play: "AI-assisted software delivery — where agents need domain context and structured workflows"
  how_to_win: "Adopt Agent Skills open standard for cross-platform portability; lean MCP surface for domain context"
  capability_strengthened: "AI agent integration — single skill format works on Claude Code, Cursor, and Windsurf"

# --- Invariants (required for core) ---
invariants: []
  # - id: "INV-001"
  #   statement: "Every Order must have at least one LineItem"
  #   enforcement: "test"
  #   fitness_function: "tests/invariants/test_order_integrity.py"

# --- Fitness Functions (required for core) ---
fitness_functions: []
  # - name: "Order integrity check"
  #   type: "integration-test"
  #   path: "tests/fitness/test_order_integrity.py"
  #   dimension: "data-integrity"

# --- ADRs ---
adrs: []
  # - "docs/adr/0001-use-event-sourcing.md"

# --- Traceability (can be auto-generated by `evospec reverse`) ---
traceability:
  endpoints: []
  tables: []
  modules:
    - "src/evospec/core/agents.py"
    - "src/evospec/mcp/server.py"
    - "src/evospec/mcp/contract_parser.py"
    - "src/evospec/templates/workflows/_context.yaml"
    - "src/evospec/cli/main.py"
    - "examples/multi-system-ux-discovery/README.md"
    - "examples/multi-system-ux-discovery/WALKTHROUGH.md"
  events: []
  migrations: []

# --- Discovery (not primary for this improvement, but included for context) ---
discovery:
  outcome: "AI agents on all platforms can use EvoSpec workflows via a single Skills format"
  opportunity: "How might we make EvoSpec workflows portable across Claude Code, Cursor, and Windsurf without maintaining 3 separate generators?"
  iteration: 1
  cadence: "weekly"
  next_checkpoint: ""
  assumptions: []
  experiments: []
  kill_criteria: ""
  kill_deadline: ""
  learnings: []

# --- Design Thinking (edge/hybrid) ---
design_thinking:
  current_phase: "prototype"
  problem_statement: "EvoSpec generates 22 platform-specific files across 3 AI platforms, but Agent Skills is now an open standard supported by all three. The MCP server also overexposes internal config data."
  ideas_explored: 3
  empathy_artifacts: []
  prototypes: []

# --- Organizational Risk (Hogan) ---
organizational_risk:
  derailer_risks: []
  cultural_constraints: []
  mitigation: ""
